name: Deploy Lotto Picker API

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Deno
        uses: denoland/setup-deno@v1
        with:
          deno-version: v1.40.x

      - name: Install Doppler CLI
        run: |
          (curl -Ls --tlsv1.2 --proto "=https" --retry 3 https://cli.doppler.com/install.sh || wget -t 3 -qO- https://cli.doppler.com/install.sh) | sh
          doppler --version

      - name: Configure Doppler
        run: |
          doppler configure set token ${{ secrets.DOPPLER_TOKEN }} --scope .
        working-directory: ./server

      - name: Run Tests
        run: |
          doppler run -- deno test --allow-net --allow-env
        working-directory: ./server

      - name: Build Application
        run: |
          # Add any build steps here if needed
          echo "Building application..."
        working-directory: ./server

      # Example deployment to a cloud provider
      - name: Deploy to Cloud
        run: |
          # This is a placeholder for your actual deployment command
          echo "Deploying to cloud..."
          # Example: doppler run -- deno run --allow-net --allow-env deploy.ts
        working-directory: ./server
